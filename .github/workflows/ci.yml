name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  windows-build:
    name: Windows build (${{ matrix.features }})
    runs-on: windows-latest
    strategy:
      matrix:
        features: [default, no-ui]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.features }}

      - name: Build release binary
        run: |
          if ("${{ matrix.features }}" -eq "no-ui") {
            cargo build --release --no-default-features --features "vigem,vjoy,ws,bluetooth"
          } else {
            cargo build --release
          }

  nix-flake-build:
    name: Nix flake build (${{ matrix.features }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features: [default, no-ui]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          key: ${{ matrix.features }}
          extra_nix_config: |
            experimental-features = nix-command flakes
            log-lines = 0

      - name: Build flake package
        run: |
          if [ "${{ matrix.features }}" = "no-ui" ]; then
            nix build "path:.#cli" --print-build-logs
          else
            nix build "path:.#ui" --print-build-logs
          fi

